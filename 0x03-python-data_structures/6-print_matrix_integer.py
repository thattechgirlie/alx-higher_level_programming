#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for y in range(len(matrix):
            for t in range(len(matrix[y])):
                if t != 0:
                    print(" ", end='')
                print("{:d}" .format(matrix[y][t]), end='')
            print()
