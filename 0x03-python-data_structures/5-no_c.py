#!/usr/bin/python3
def no_c(my_string):
    new_string = ""
    for t in range(len(my_strring)):
        if my_string[t] != is 'c' and my_string[t] != 'C':
            new_string += my_string[t]
        return new_string
